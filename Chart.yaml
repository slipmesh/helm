apiVersion: v2
name: slipmesh-network
description: >-
  Full-mesh AmneziaWG overlay (mesh), BIRD OSPF/iBGP routing + bridge CNI conflist (router),
  and NAT/masquerade firewall (nftables) for the slipmesh.net operators, deployed as
  DaemonSets across a full mesh of Talos nodes.
type: application
version: 0.2.1
appVersion: "0.2.1"
home: https://github.com/slipmesh/helm
sources:
  - https://github.com/slipmesh/helm
  - https://github.com/slipmesh/operators
